# Campus KB RAG Web

Vue 3 + Vite + Element Plus frontend for the RAG demo.

## Dev

- Ensure backend runs at http://localhost:8080
- Start dev server:

```bash
npm install
npm run dev
```

If you deploy frontend on a different domain, enable CORS on the backend.
